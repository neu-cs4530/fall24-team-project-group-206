import React from 'react';
import './index.css';

/**
 * CommunityInfo component which displays the community (if applicable) that they are in.
 */
const CommunityInfo = () => (
  <div className='community-info'>
    <p>[insert community]</p>
  </div>
);

export default CommunityInfo;
